"""Content-parity gate self-test fixture (antigravity side).

This file is deliberately, permanently divergent from its claude-side
counterpart so the content-parity gate can prove it detects divergence.
It is NOT modeled on any real mirrored file and never needs to track the
real tree — the divergence below is the whole point.
"""


def greeting() -> str:
    return "hello from the antigravity side"
