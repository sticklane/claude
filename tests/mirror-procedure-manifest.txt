# Mirror procedure-coverage manifest (spec mirror-procedure-discipline, R3/R4).
#
# One entry per line, pipe-delimited: <source file>|<mirror file>|<critical phrase>
# tests/test_mirror_procedure_coverage.sh fails when a phrase is present in the
# source but absent from the mirror. Blank lines and lines starting with # are
# skipped, so audits (R5/R6) append "# checked: <skill> — <finding or clean>"
# comment lines here to record a side-by-side read that found nothing to seed.
#
# Seeded from the 6 manifest-expressible of this session's 8 fixes (commits
# e742cb6, 4d1edcc). The other 2 are the rule's named blind spots: build.md's
# pure reorder (ordering-only) and codex-autopilot's content-swap (mirror-adds).
.claude/skills/autopilot/SKILL.md|antigravity/.agents/workflows/autopilot.md|bounded goal
.claude/skills/drain/reference.md|antigravity/.agents/workflows/drain.md|Environment kill
.claude/skills/idea/SKILL.md|antigravity/.agents/skills/idea/SKILL.md|one focused question each
.claude/skills/breakdown/SKILL.md|antigravity/.agents/workflows/breakdown.md|recording a BLOCKED flip
.claude/skills/critique/SKILL.md|antigravity/.agents/workflows/critique.md|Relay the verdict and findings verbatim
.claude/skills/drain/reference.md|antigravity/.agents/workflows/drain.md|credentials or external services beyond
.claude/skills/design/SKILL.md|antigravity/.agents/skills/design/SKILL.md|expensive to reverse
.claude/skills/design/SKILL.md|antigravity/.agents/skills/design/SKILL.md|would removing this cause mistakes
# checked: design — restored two dropped decision conditions (prototype reversibility/cost gate, AGENTS.md "would removing this cause mistakes?" test); workflows/design.md is a pointer, reference.md diffs are load-bearing runtime renames
.claude/skills/distill/SKILL.md|antigravity/.agents/skills/distill/SKILL.md|directory-named command
.claude/skills/distill/SKILL.md|antigravity/.agents/skills/distill/SKILL.md|procedures as checklists
# checked: distill — restored two dropped §3 New-skills authoring instructions ("directory-named command", "procedures as checklists"); workflows/distill.md is a pointer; verifier/critic→review, AskUserQuestion→ask, CLAUDE.md/rules→AGENTS.md and the added workflow route-table row are load-bearing runtime renames; absent Next stage line is a systematic port omission (14 source skills carry it, port carries 3; design audit left its own absence too)
.claude/skills/evals/SKILL.md|antigravity/.agents/workflows/evals.md|skill-authoring gap
# checked: evals — full side-by-side read of workflows/evals.md (only mirror surface; no .agents/skills/evals/ dir). Restored two incidental drops: the "if a failure exposed a skill-authoring gap, distill the lesson" closing step (distill workflow exists in the port so it resolves), and the scaffold "model it on the /breakdown scenario" shape pointer (adapted off the Claude-Code-only reference.md to the shared evals/breakdown/ scenario dir). teardown.sh omission re-confirmed load-bearing: agy -p is live-tested UNSAFE for isolated use (runtimes/antigravity.md), so evals runs manually via Agent Manager with no automated runner, shifting teardown to the human operator. Other diffs load-bearing: run.sh→manual provision+Agent-Manager launch, EVAL_DIR/timeout-900/MAX_TURNS/EVALS_ROOT runner env vars dropped (no runner), allowed-tools.txt ignored, added bash-3.2 caveat. See Decisions in task 04 re whether the manual flow should also name operator-run teardown.
# checked: factcheck — full side-by-side read of antigravity/.agents/skills/factcheck/SKILL.md vs source .claude/skills/factcheck/SKILL.md; procedures equivalent (same contract, same §1/§2/§3 steps, same conditions, same order), no incidental drift to seed. All divergences load-bearing/non-procedural: worker type (general-purpose agents with WebSearch/WebFetch → separate web-capable Agent Manager conversations, since Antigravity has no general-purpose web agent), tier-language rename (Haiku → scout-tier), an added "Tier and cap follow AGENTS.md Dispatch authoring" pointer (cross-ref, not procedure), and routing-note prose rephrase. No antigravity/.agents/workflows/factcheck.md exists — skills-dir mirror only. (Mirror reference.md exists but is out of this task's Touch scope.)
.claude/skills/drain/SKILL.md|antigravity/.agents/workflows/drain.md|agentprof:stage=critique-intake
.claude/skills/drain/SKILL.md|antigravity/.agents/workflows/drain.md|agentprof:stage=stub-intake
.claude/skills/drain/SKILL.md|antigravity/.agents/workflows/drain.md|agentprof:stage=auto-breakdown
# checked: drain agentprof stage-marker instrumentation — antigravity's workflow implemented critique intake, stub intake, and 4a auto-breakdown but never emitted their agentprof:stage markers (source and the codex mirror already did), leaving those stages unattributed in profiling. Added the three missing markers to antigravity/.agents/workflows/drain.md and seeded them here so a future antigravity edit can't silently drop them again. Also added the previously-missing source SKILL.md 3b auto-breakdown marker itself (mirrored into codex) — not manifest-expressible since it's a same-file gap, not a source/mirror divergence.
.claude/skills/gate/SKILL.md|antigravity/.agents/skills/gate/SKILL.md|delegate the run to a subagent
# checked: gate — full side-by-side read of antigravity/.agents/skills/gate/{SKILL.md,reference.md} vs source .claude/skills/gate/{SKILL.md,reference.md}; workflows/gate.md confirmed a thin pointer (real content is skills/gate/). Preliminary note CORRECTED-as-not-a-bug: absence of a bin/install-gates citation is load-bearing — Antigravity uses .agents/hooks.json + .agents/hooks/*.sh (name-keyed JSON, no shared installer possible), so the SKILL defers install specifics to its own reference.md rather than the git-specific installer. Restored ONE incidental drop (source step-1 "delegate the run to a subagent — raw logs don't belong in the main context", seeded above). Everything else load-bearing: no per-turn Stop gate (Antigravity's Stop event fires at session end → substitutes artifact-review + verifier + CI, per reference.md "What replaces the Stop gate"), command-blocking PreToolUse hook (Antigravity analogue of Claude Code's permission deny rules / Terminal Execution Policy), event-keyed→name-keyed hook JSON + tool-name renames (write_to_file/replace_file_content, run_command), $ARGUMENTS/CLAUDE.md→user/AGENTS.md, exit-code-2 semantics→allow_tool JSON. Session-scoped alternatives (/goal, prompt/agent-type hooks) and the "don't mix exit-code and JSON" sanity rule are Claude-Code hook mechanisms with no Antigravity equivalent — load-bearing omissions. Absent Next stage/autopilot close line is the same systematic port omission already recorded in the distill/design audits (14 source skills carry it, port carries 3), not a gate-specific gap.
# checked: handoff — full side-by-side read of antigravity/.agents/skills/handoff/SKILL.md + workflows/handoff.md vs source .claude/skills/handoff/SKILL.md; procedures equivalent (same intro→refresh-path→steps 1-4 order, same 5 HANDOFF.md fields Task/State/Files-touched/Gotchas/Verification, same conditions: no-task-file fallback, commit-if-tree-dirty, distill-first-if-corrections), no incidental drift to seed. All divergences load-bearing/non-procedural: session→conversation runtime language, /clear→"start a new conversation", "Run /distill"→"Apply the distill skill", token-discipline.md→AGENTS.md citation rename, .claude/HANDOFF.md→repo-root fallback (no .claude dir in Antigravity), plus prose compression of the intro re-billing rationale and step-1 "no conversation history" (subsumed by "no narrative"). Absent Next stage close line is the same systematic port omission already recorded in the distill/design/gate audits (14 source skills carry it, port carries 3), not a handoff-specific gap. workflows/handoff.md is a thin pointer + refresh-path summary, consistent with the skill.
# checked: list-specs — full side-by-side read of antigravity/.agents/skills/list-specs/SKILL.md vs source .claude/skills/list-specs/SKILL.md; procedures equivalent (same intro contract: read-only, no arguments, scans specs/ under cwd, never auto-invoke the Next command; same steps 1-3 in order: run bundled list_specs.py from repo root → relay stdout verbatim as the `| Spec | Status | Next command |` table alphabetical by slug, same `no specs/ directory found` fallback → do not run/queue/offer the Next command unless the user asks in a follow-up; same Next stage: none), no incidental drift to seed. Sole divergence load-bearing: step-1 script path `.claude/skills/list-specs/list_specs.py` (+ plugin-relative fallback) → `<this skill dir>/list_specs.py`, since the Antigravity skill dir path differs and a hardcoded Claude path would be wrong; everything else is prose rewording (unaltered→verbatim, etc.). No antigravity/.agents/workflows/list-specs.md exists — skills-dir mirror only. The bundled list_specs.py is byte-identical between source and mirror (covered by codequality-antigravity-content-parity's byte-diff gate, out of this prose-procedure task's scope, nothing to re-fix).
.claude/skills/onboard/SKILL.md|antigravity/.agents/skills/onboard/SKILL.md|the repo's check command
# checked: onboard — full side-by-side read of antigravity/.agents/skills/onboard/SKILL.md vs source .claude/skills/onboard/SKILL.md (workflows/onboard.md confirmed a thin pointer, real content in skills/). Restored TWO incidental drops: (1) source §3 conventions item "and the repo's check command" was dropped from the mirror's folded conventions/gotchas bullet — seeded above; (2) source §2 caution-2 offers "pipe through `tail`, or delegate the runs to a subagent" but the mirror had collapsed it to "pipe through `tail` and record", losing the delegation alternative — restored as "or delegate the runs to a separate conversation" (subagent→separate-conversation is the mirror's own §1 delegation idiom, so it's a runtime rename, not manifest-expressible). Everything else load-bearing: the whole AGENTS.md+CLAUDE.md orientation split → single AGENTS.md (Antigravity's native context file, no separate rules file to bridge), @AGENTS.md bridge line dropped, `.claude/settings.json` permissions allowlist §4 → Terminal Execution Policy Guardrails (no checked-in permissions file), scout parallel Agent-tool fan-out → "apply the scout skill in a separate cheap conversation", `.claude/`/CLAUDE.md config names → AGENTS.md/GEMINI.md/`.agents/`, /gate Stop-hook+bin/install-gates → PostToolUse `.agents/hooks.json`, REVIEW.md code-review tuning → Artifact review policy check, migration paragraph adapted to no-CLAUDE.md. Absent `Next stage:` close line is the same systematic port omission already recorded in the distill/design/gate/handoff audits (14 source skills carry it, port carries 3), not an onboard-specific gap.
# checked: prioritize — full side-by-side read of antigravity/.agents/workflows/prioritize.md vs source .claude/skills/prioritize/SKILL.md; procedures equivalent (same 4 top-first contract bullets; same steps 1-6 in order with the same conditions: scanner `nothing to reprioritize` early-stop → no interview/no commit, one free-form question with the verbatim R3 quote + terminal-client lead-in-line rationale, validate each named change as Ref-in-step-1-table AND target normalizes to P0-P3 else `not applied`/never-guessed, "none"/equivalent → skip to step 6, four-branch Priority:-header edit rule "touch no other line", commit-only-when-≥1-applied with the exact `chore: reprioritize <N> task(s) across <M> spec(s) per interview` message and <N>/<M> definitions, report + clean exit), no incidental drift to seed. No antigravity/.agents/skills/prioritize/SKILL.md exists — that skills dir holds only the bundled prioritize_scan.py + README + test; the SKILL body is mirrored as the human-launched workflow (load-bearing port shape: prioritize is an execution stage and Antigravity's workflow format IS the human-launch primitive). All divergences load-bearing/non-procedural: the source "Launch authorization (hard rule)" anti-injection block folds into "human-launched workflow" (the port systematically drops the explicit block from execution-stage workflows — autopilot.md and drain.md do the same, the workflow format itself gating launch), `AskUserQuestion` → "fixed-option picker" runtime rename with the AskUserQuestion-specific "caps at 4 options" reason dropped (runtime-agnostic "can't represent an arbitrary re-ranking" rationale retained), scanner path `<this skill dir>/prioritize_scan.py` → mirrored `.agents/skills/prioritize/prioritize_scan.py`, and the R4/R5/R6 step-header provenance tags dropped (non-procedural spec-requirement labels; R3 kept in both). The bundled prioritize_scan.py is `.py` scope for codequality-antigravity-content-parity's byte-diff gate, out of this prose-procedure task's scope — nothing to re-fix here.
.claude/skills/prose-review/reference.md|antigravity/.agents/skills/prose-review/reference.md|associated with (not proven to cause)
.claude/skills/prose-review/reference.md|antigravity/.agents/skills/prose-review/reference.md|explicitly *rewarded* property here
.claude/skills/prose-review/reference.md|antigravity/.agents/skills/prose-review/reference.md|This is guidance, not a rule
.claude/skills/prose-review/reference.md|antigravity/.agents/skills/prose-review/reference.md|announces duplicate link text
.claude/skills/prose-review/reference.md|antigravity/.agents/skills/prose-review/reference.md|Acronym and jargon on first use
.claude/skills/prose-review/reference.md|antigravity/.agents/skills/prose-review/reference.md|79% of readers scan rather than read
.claude/skills/workboard/SKILL.md|antigravity/.agents/skills/workboard/SKILL.md|Pass explicit roots when the user names directories
.claude/skills/workboard/SKILL.md|antigravity/.agents/skills/workboard/SKILL.md|user asks what a state means
# checked: workboard — full side-by-side read of antigravity/.agents/skills/workboard/SKILL.md vs source .claude/skills/workboard/SKILL.md (no antigravity/.agents/workflows/workboard.md; skills-dir mirror only, re-confirmed at task start — bundled workboard.py/test_workboard.py are byte-identical per docs/memory/workboard-mirror-verbatim.md and out of this prose-procedure task's scope). Restored TWO incidental §2 drops, seeded above: (1) the "Pass explicit roots when the user names directories" instruction dropped from the No-ROOTS bullet, and (2) the "or the user asks what a state means" condition dropped from the reference.md-load trigger. Everything else load-bearing/non-procedural: description /fleet→Agent-Manager surface note and "Kiro/Antigravity"→"Antigravity conversation" runtime rename; intro docs/agent-dashboards.md relative link → "toolkit repo's ... — not shipped with installs" (cross-ref that can't resolve in the antigravity tree); §1 fallback "<scratchpad>"→"/tmp" and "present"→"open" (runtime temp-dir convention); §2 added "~/.gemini/antigravity*/brain/ conversation artifacts" sentence (accurate antigravity-runtime scanner context) and added collapsible spawn-tree paragraph (descriptive of the shared agent-console dashboard, not a procedural step); §3 "fresh session"→"fresh Agent Manager conversation", "re-dispatch via /build or /drain"→"re-dispatch", "run the verifier"→"verify", "resume it in Antigravity"→"resume it in the Agent Manager" (Antigravity dispatch/verify primitives); closing "Next stage: ... /build, /drain, /handoff, or the verifier agent"→"Next step: ... the build/drain/handoff workflows" (systematic port shape). §1 launch block and §4 session-hygiene identical.
# checked: prose-review — full side-by-side read of antigravity/.agents/skills/prose-review/{SKILL.md,reference.md} vs source (no workflows/prose-review.md; skills-dir mirror only, confirmed). reference.md was missing all six commit-2d2528a doctrine additions (the mirror predates that commit) — ported verbatim and seeded above: (1) "## Why this matters" DORA correlational-not-causal citation, (2) item-7 NN/g Morkes & Nielsen conciseness-rewarded citation, (3) Google-style "guidelines not rules" priority-when-guides-conflict framing, (4) descriptive-link-text "Learn More" bad example + screen-reader accessibility rationale, (5) new acronym/jargon-on-first-use convention with the NN/g non-linear-scanning tension, (6) reader-test inverted-pyramid + subheading-quality structural probes + the 79%-scan citation. All six are advisory doctrine with external-URL citations that resolve identically under Antigravity — pure incidental drops, nothing runtime-forced. SKILL.md procedures equivalent; its two divergences are load-bearing: reader-test dispatch "spawn a fresh-context agent (…token-discipline.md)" → "open a fresh Agent Manager conversation (…AGENTS.md)" (Antigravity dispatch primitive + doc-map rename), and invocation-gate "model-invocable" → "safe to auto-trigger" (same procedural condition: read-only mode auto-fires, --fix human-typed-only — no disable-model-invocation frontmatter mechanism in Antigravity to name).
.claude/skills/build/SKILL.md|codex/.agents/skills/build/SKILL.md|surface to the user when attended
# checked: codex-build — three-way read of codex/.agents/skills/build/SKILL.md against source .claude/skills/build/SKILL.md AND the already-reconciled antigravity/.agents/workflows/build.md (commit 4d1edcc). Restored ONE incidental dropped condition, seeded above: the step-4 pre-commit-review disposition of un-fixable findings collapsed from source's two-branch "surface to the user when attended, or add to `Discovered:` when unattended" down to bare "surface to the user." — both source and the antigravity mirror keep the attended/unattended branch, nothing codex-specific forces the drop, and codex build's procedure IS run by drained/unattended workers so the unattended branch is live. Manifest canary is "surface to the user when attended" because the fuller "add to `Discovered:` when unattended" wraps across a line break in the read-only source (grep -F is line-oriented). Everything else procedurally equivalent; all remaining divergences load-bearing or non-procedural: launch-authorization block rewritten for `allow_implicit_invocation: false` (Codex's real gating primitive, no trigger phrases / no self-invoke pathway), `/simplify`/`/code-review`/`/handoff`/`/clear`/`/drain`/`/distill` → `$simplify`/`$code-review`/`$handoff`/"clear the session"/`$drain`/`$distill` (Codex invocation syntax), "per CLAUDE.md" → "per CLAUDE.md/AGENTS.md" (Codex reads AGENTS.md), startup-sweep "claude agents --json / drain mechanism" → "the runtime's session list" (runtime rename), and prose compression of rationale asides that carry no procedural step/condition/count (step-0 agentprof rationale — already abbreviated in source steps 1-4; "the attended parallel to the worker's decision-deferral rule"; "a re-verify overwrites it"; "the observed stall mode in drained runs"; "This is the escape available to an attended run"; "breakdown's grammar"; "a worker that wrote HUMAN.md would fail drain's merge-time Touch whitelist"; "the simplification pass already ran"; "this is a printed pointer, not a loop"). Steps 0-4 (load, plan, implement, verify, close-out) otherwise present in the same order with the same decision points.
.claude/skills/drain/reference.md|codex/.agents/skills/drain/SKILL.md|majority PASS
.claude/skills/drain/reference.md|codex/.agents/skills/drain/SKILL.md|lowest angle index
.claude/skills/drain/SKILL.md|codex/.agents/skills/drain/SKILL.md|ends here, not at step 2
.claude/skills/drain/SKILL.md|codex/.agents/skills/drain/SKILL.md|sends you here unconditionally
.claude/skills/drain/SKILL.md|antigravity/.agents/workflows/drain.md|after EVERY recorded verdict
# checked: factcheck — reference.md — full side-by-side read of antigravity/.agents/skills/factcheck/reference.md vs source .claude/skills/factcheck/reference.md; procedures equivalent (same primary/secondary source rubric + tie-breakers, same worker-prompt template with the same 5 return fields Claim/Verdict/URL/Quote/Confidence and same Rules block, same clustering counts 1/3–10 calls + 2–4 workers, same two-section Findings/UNVERIFIED assembly, same ≤30-word quote and ≤250-word output caps, same step order), no incidental drift to seed — confirms the stub-intake preliminary read. All divergences load-bearing or non-procedural prose: worker type general-purpose agent → web-capable Agent Manager conversation (Antigravity has no general-purpose web agent), cross-ref .claude/rules/token-discipline.md → AGENTS.md, tier language Haiku → scout-tier, and pure synonyms LLM→model / Launch→Open / "inside this task"→"inside this conversation" / "effort: medium"→"medium effort" (no dropped step, condition, count, or reordered decision). No antigravity/.agents/workflows/factcheck.md exists — skills-dir mirror only.
# checked: codex-drain — three-way read of codex/.agents/skills/drain/SKILL.md against source .claude/skills/drain/SKILL.md + reference.md AND the antigravity/.agents/workflows/drain.md reference point. Restored TWO incidental divergences, seeded above. (1) TOURNAMENT RANK TIE-BREAK: codex compressed the whole filter+rank to "rank their results" with no deterministic ordering — a codex orchestrator would tie-break arbitrarily, producing inconsistent tournament outcomes. Restored the filter (three verifier runs per DONE candidate, survives only on majority PASS two-of-three, verifier-BLOCKED disqualifies) and the mechanical rank chain (most PASS votes → fewest gate findings → smallest total diff → lowest angle index), matching reference.md's "Tournament" and antigravity's Filter/Rank bullets; nothing in Codex's runtime forces dropping the tie-break. (2) STEP-3/3a LOOP-BACK GATE post commit d35fc9e: codex carried the pre-fix discretionary phrasing ("Loop to step 2 while anything is dispatchable" + step-3a "At each safe boundary"), the exact wording d35fc9e replaced in the Claude source after a transcript audit found a generation running 9 verdicts with the 3a baton check never evaluated. Ported the fix's procedural intent: step-3 closing now forces the 3a check before any loop-back ("Every recorded verdict ends here, not at step 2 … process violation"), and step-3a opening states it fires after EVERY recorded verdict unconditionally. Claude source is primary per the task; antigravity's drain.md still carries the pre-d35fc9e phrasing too (fix landed Claude-only so far) — a separate antigravity-leg gap, reported as Discovered, not touched here. CONSISTENT, no fix: the `max(2, 6 − W)` baton formula and its W=1→5/W=3→3/W=5→2 mapping, and the canonical push-guard (push only with configured upstream, never --force, rejected/offline warns-and-continues, applies to every drain commit) are restated equivalently across all three bodies. NOT ported (codex's own compression, flagged Discovered): the tournament's generate angle-suffixes, merge-next-ranked-survivor, and DEFERRED-beats-failed verdict routing remain compressed in codex — beyond this task's flagged rank-tie-break scope, left for a follow-up decision rather than a rewrite. All other divergences load-bearing: `allow_implicit_invocation: false` launch gating, `$drain`/`$critique`/`$breakdown`/`$distill` invocation syntax, "the runtime's session list"/Agent-Manager naming surface, and reference.md→inline-section folding (codex drain is single-file, no reference.md).
