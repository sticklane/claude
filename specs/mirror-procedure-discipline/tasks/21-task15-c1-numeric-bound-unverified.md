Status: draft
Intake-refused: gate — assessor's OBSOLETE verdict is the wrong label: this stub's gap (task 15's C1 numeric target) is still genuinely OPEN, not closed, so it can't cite closing evidence; it duplicates task 15's own Unblock: ask: line with no independent action. Human should close this by hand (no `duplicate` status exists in drain's enum) once task 15's Unblock: ask: resolves, not promote it (2026-07-13)
Discovered-from: specs/mirror-procedure-discipline/tasks/15-normalize-next-stage-lines.md
Spec: ../SPEC.md
Blocking: no

# Task 15 criterion 1's ≥14 target was an unverified numeric bound

Task 15's acceptance criterion 1 required `Next stage:` count ≥14,
assuming 14 antigravity mirrors exist for the 14 source skills that carry
the line. Only 13 mirrors exist — `workflow-author` is Claude-Code-only
(ultracode Workflow tool) with no antigravity analog, so 14 was never
achievable. This is the same fact task 15 is currently `blocked` on
(`Unblock: ask:` there) — likely obsolete once that question is answered;
recorded here only because the worker's verdict reported it as a
`Discovered:` entry, which drain materializes mechanically. A human
triaging this stub should most likely close it as duplicate/obsolete
alongside resolving task 15 directly, rather than promote it separately.

## Acceptance

<!-- draft: needs runnable criteria before promotion -->
