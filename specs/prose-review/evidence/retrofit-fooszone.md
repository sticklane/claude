# Retrofit evidence: ~/fooszone orientation docs

Task: specs/prose-review/tasks/06-retrofit-fooszone.md (spec R9).
Target docs: `~/fooszone/README.md` and `~/fooszone/AGENTS.md` (AGENTS.md
present, so `docs/*.md` not in scope). Work done in a `git worktree` of
`~/fooszone` on branch `task/06-retrofit-fooszone`, leaving the live checkout
untouched (cross-repo pattern, docs/memory/drain-dispatch-lessons.md).

## CI precondition check (before any commit) — PASS

Checked `~/fooszone/.github/workflows/*.yml`. Three push-triggered workflows,
all docs-safe for a root-markdown-only commit:

- **ci-node.yml** — `on: push: branches:[main]` with `paths-ignore` covering
  `**.md`, `docs/**`, `specs/**`, `.claude/**`. A README.md/AGENTS.md-only
  push is explicitly skipped.
- **ci-go.yml** — `on: push: branches:[main]` with an allowlist `paths:`
  filter (`go/**`, `spec/tornado_table.json`, `src/generated/tableSpec.ts`,
  scripts, own workflow). Root `README.md`/`AGENTS.md` match none of the
  includes, so the workflow does not trigger.
- **ci-python.yml** — allowlist `paths:` filter (`ml/**`, own workflow). Root
  markdown matches nothing; does not trigger.

Conclusion: a commit touching only `README.md` + `AGENTS.md` triggers no
metered CI. Precondition satisfied → proceed to commit.

## Vale setup

Vale 0.x at `/opt/homebrew/bin/vale`, config `~/.vale.ini` (written by the
toolkit's `bin/install-vale`; StylesPath resolved, `Vocab = House`,
`Packages = Google`, `BasedOnStyles = Vale, Google` for `*.md`). fooszone has
no repo-local `.vale.ini`, so the toolkit's global config applies — the same
resolution the `vale/.vale.ini.template` documents. Command: `vale <file>`.

## Vale counts — BEFORE → AFTER

| File | Errors | Warnings | Suggestions | Total |
| --- | --- | --- | --- | --- |
| README.md before | 5 | 10 | 14 | 29 |
| README.md after  | 3 | 4  | 14 | 21 |
| AGENTS.md before | 4 | 8  | 27 | 39 |
| AGENTS.md after  | 4 | 6  | 26 | 36 |
| **Combined before** | **9** | **18** | **41** | **68** |
| **Combined after**  | **7** | **10** | **40** | **57** |

Resolved by the edits:
- README: 2 `Google.EmDash` errors (spaced em-dashes → unspaced, per the
  house standard adopted in prose-review/03) and 6 `Google.Headings`
  sentence-case warnings (`Web App`→`Web app`, `Go Pipeline`→`Go pipeline`,
  `Local Development`→`Local development`, second `Web App`, second `Go
  Pipeline`, `Project Structure`→`Project structure`). Also standardized the
  three ML-model bullet dashes (` -- ` → `—`) for internal consistency.
- AGENTS: 2 `Google.Headings` warnings (hand-authored `Quick Reference`→`Quick
  reference`, `Non-Interactive Shell Commands`→`Non-interactive shell
  commands`) and 1 `Google.Passive` suggestion (`may be aliased` reworded to
  active voice).

## Rubric pass (nine-item AI-antipattern rubric) — BEFORE → AFTER

BEFORE findings:
1. **README** — largely rubric-clean. Only inconsistency: em-dash style
   mixed ` -- ` (ML-model bullets) with spaced ` — ` (lines 76, 125), a mild
   item-7 (inconsistent/redundant styling) nit. The ML-model bullet list is a
   genuinely discrete enumeration → item-1 carve-out, not flagged.
2. **AGENTS** — item 7 (repetitive/redundant information): the Beads
   `Quick Reference` command block (`bd ready`/`bd show`/`bd update
   --claim`/`bd close`) appears **three times** — once hand-authored (top),
   twice inside tool-generated marker blocks (`<!-- BEGIN BEADS INTEGRATION
   -->` and `<!-- BEGIN BEADS CODEX SETUP -->`). Item 4 (over-formatting):
   heavy CAPS-for-emphasis (`ALWAYS`, `MUST`, `ALL`, `NEVER`) — but all of it
   sits inside the generated blocks except the single hand-authored
   `**ALWAYS use non-interactive flags**` (softened to `Always`).

AFTER state:
- README: em-dash style unified to unspaced `—`; no rubric antipatterns
  remain in hand-authored prose.
- AGENTS: hand-authored heading casing fixed and the one hand-authored CAPS
  emphasis softened. **The item-7 triple-repeat is NOT edited**: two of the
  three blocks live inside `<!-- BEGIN BEADS … -->` / `<!-- END BEADS … -->`
  markers — tool-generated content regenerated by `bd setup`, which is
  machine-managed and outside /prose-review's charter (it reviews
  human-facing prose, not machine-generated/parsed content). Editing them
  would be reverted on the next `bd setup` run and risks breaking the
  integration. Recorded as a finding for the human: the redundancy is a
  tooling artifact (repo carries a hand-written Beads intro AND two generated
  Beads blocks); resolving it is a content/tooling decision (drop the
  hand-written intro, or one of the generated blocks), not a prose rewrite.

## Reader test (inline cold read)

- README — *What is this?* Clear from the one-sentence lead (CV/ML foosball
  analysis system). *First action?* Obvious: `## Local development` →
  `npm install`. *Stumbles:* the ASCII architecture diagram is dense but
  legible. *Unanswered:* none blocking.
- AGENTS — *What is this?* Agent instructions for `bd`/beads issue tracking.
  *Stumbles:* the three near-identical Beads blocks make it ambiguous which
  is authoritative (the item-7 finding above); a fresh agent could follow any
  of them, which is harmless since they agree, but the repetition is real.

## Residual Vale findings — itemized, justified as domain jargon / out-of-scope

README.md (3 errors, 4 warnings, 14 suggestions remain):
- `Vale.Spelling`: **foosball** (3:59), **dev** (50:306, i.e. "dev server"),
  **pretraining** (60:83) — all domain/ML jargon; `foosball` is the entire
  subject domain. Not fixed.
- `Google.Headings`: **FoosZone** (1:3, product name), **ML models** (58:5),
  **ML training** (88:5) — `ML` is a retained initialism; Vale wants `Ml`,
  which is wrong. False positives for an initialism/proper noun. Not fixed.
- `Google.Parens` / `Google.Semicolons` (suggestion level, lines 44/50/54/56/
  60/75/86/123) — parenthetical definitions (`(offline analysis)`,
  `(OpenCV bindings)`, `(homography.ts)`) and compound technical sentences;
  judged clearer as written. Not fixed.
- `Google.Passive` (suggestion, 50/119/123): `was retired`, `is uploaded`,
  `is served` — agentless technical passive where the actor is irrelevant.
  Not fixed.
- `Google.Colons` (117:44, `: C`) — inside the deploy code-context comment;
  not prose. Not fixed.

AGENTS.md (4 errors, 6 warnings, 26 suggestions remain):
- Hand-authored residuals (3): `Vale.Spelling` **env** (37:44, "env var" —
  standard shell jargon); `Google.Parens` on `(beads)` (3:26) and
  `(interactive)` (19:77) — definitional parentheticals, kept.
- **All other AGENTS residuals (lines 40–108) are inside the tool-generated
  Beads marker blocks** (`Google.EmDash` 55/57, `Google.Acronyms` CAPS
  55–83, `Google.Contractions` 55/63/81/104/106, `Google.Headings` 40/44/61/
  88/92, `Google.WordList` CLI 90:253, `Google.Semicolons` 108, `Vale.Spelling`
  **hoc** 106 from "ad hoc"). Machine-generated content, outside
  /prose-review's edit charter and regenerated by `bd setup`. Not fixed by
  design.

## Files changed

- Target repo (`~/fooszone`, branch `task/06-retrofit-fooszone`): `README.md`,
  `AGENTS.md`.
- Toolkit (branch `task/06-retrofit-fooszone`): this evidence file +
  the task file close-out.
