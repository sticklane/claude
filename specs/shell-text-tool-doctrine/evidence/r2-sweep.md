# R2 sweep: skill-embedded shell text commands vs `.claude/rules/shell-text-tools.md`

Swept every `sed`/`awk`/`grep`/`cat` occurrence in
`.claude/skills/**/SKILL.md` and `.claude/skills/**/reference.md`
(35 matches across 11 files) and classified each against the R1 rule.

Verdict legend:
- **ok** — already conformant: read-only extraction with bounded output
  (`-q`/`-c`/`-l`/`-m`, anchored line-start, `awk` anchored range, `head`
  cap), a prose mention of the word (not a live command), or a `cat` stdin
  capture (not a file read/write).
- **bounded-fix** — a read-shaped example whose output was unbounded and was
  capped/anchored per R1(c).
- **write-violation** — an in-place shell write (`sed -i`, `cat >` onto a
  tracked file) that is not a documented R1 exception; removed or converted
  to Edit/Write.

## Per-file summary (one row per swept file)

| File | Usages | Verdict | Note |
|------|--------|---------|------|
| `.claude/skills/evals/SKILL.md` | l41 | ok | `grep -q` transcript assertion, bounded |
| `.claude/skills/evals/reference.md` | l39,45,106-109,113,147 | ok | `cat >` heredocs = R1 generated-fixture exception; all greps `-q`/anchored, awk anchored range |
| `.claude/skills/breakdown/SKILL.md` | l115,130,135,143 | ok | anchored `grep -q`, prose grep mentions, single-file `git show \| grep` template |
| `.claude/skills/idea/SKILL.md` | l118,121,125,141,147 | ok | `grep -ci` count check + prose grep mentions |
| `.claude/skills/drain/SKILL.md` | l85,86 | ok | anchored header extraction `grep '^Status:'` / `grep '^Depends on:'` |
| `.claude/skills/drain/reference.md` | l800-802,940,1228 | ok | bounded-grep authoring guidance (prose), `git log --grep` filter |
| `.claude/skills/gate/reference.md` | l33,43,53,172,173 | ok | `grep -qE` anchored, `INPUT=$(cat)` stdin capture, `head -1 \| grep -qE`; l172-173 `sed -i`/`cat >` are deny-rule ADVICE text = R1 exception |
| `.claude/skills/workflow-author/SKILL.md` | l48 | ok | "grep-like scouting" prose |
| `.claude/skills/workflow-author/reference.md` | l167 | ok | "grep-like scouting" comment prose |
| `.claude/skills/ctx/SKILL.md` | l3 | ok | description prose "grep/Grep fits that" |
| `.claude/skills/build/SKILL.md` | l286,292 | ok | `grep -l '^Status: pending'` bounded+anchored |

Result: **11/11 files ok** — zero bounded-fix, zero write-violation. The
toolkit's skills were already authored with bounded, anchored extraction; R1
codifies the existing practice rather than correcting it.

## Documented R1 write-side exceptions (not violations)

- `evals/reference.md:39,45` — `cat > src/greet.sh`, `cat > specs/demo/SPEC.md`
  build eval-sandbox fixtures (generated/derived files, regenerated by the
  eval harness) — R1(a) generated-files exception.
- `gate/reference.md:172-173` — `sed -i` / `cat >` / `Bash(sed -i *)` appear
  inside prose that ADVISES pairing the Stop hook with permission `deny`
  rules; they are advice text describing what to deny, not usages the skill
  performs.

## Per-usage detail

| File:line | Command (abbrev) | Shape | Verdict |
|-----------|------------------|-------|---------|
| evals/SKILL.md:41 | `grep -q '"subagent_type":"scout"'` | read/bounded | ok |
| evals/reference.md:39 | `cat > src/greet.sh <<'EOF'` | write/fixture | ok (R1 exception) |
| evals/reference.md:45 | `cat > specs/demo/SPEC.md <<'EOF'` | write/fixture | ok (R1 exception) |
| evals/reference.md:106 | `grep -q "^Status:"` | read/anchored | ok |
| evals/reference.md:107 | `grep -q "^Depends on:"` | read/anchored | ok |
| evals/reference.md:108 | `grep -q "^## Acceptance"` | read/anchored | ok |
| evals/reference.md:109 | `awk '/^## Acceptance/.../^## /' \| grep -q` | read/anchored-range | ok |
| evals/reference.md:113 | `grep -q "Parallelization"` | read/bounded | ok |
| evals/reference.md:147 | `grep -Eq '"subagent_type"...scout'` | read/bounded | ok |
| breakdown/SKILL.md:115 | `grep -q '^Status: done'` | read/anchored | ok |
| breakdown/SKILL.md:130 | prose "grep for the concepts" | prose | ok |
| breakdown/SKILL.md:135 | `git show <base>:<path> \| grep version` | read/single-file template | ok |
| breakdown/SKILL.md:143 | prose "an L0 grep" | prose | ok |
| idea/SKILL.md:118 | prose "grep/count-based criterion" | prose | ok |
| idea/SKILL.md:121 | prose "grep- or count-based" | prose | ok |
| idea/SKILL.md:125 | `grep -ci '<phrase>'` | read/count | ok |
| idea/SKILL.md:141 | prose "an L0 grep" | prose | ok |
| idea/SKILL.md:147 | prose "a grep pose as proof" | prose | ok |
| drain/SKILL.md:85 | `grep '^Status:'` | read/anchored | ok |
| drain/SKILL.md:86 | `grep '^Depends on:'` | read/anchored | ok |
| drain/reference.md:800 | prose `! cmd \| grep -q` guidance | prose | ok |
| drain/reference.md:801 | prose about `grep -c` exit code | prose | ok |
| drain/reference.md:802 | prose `grep -c … \|\| true` | prose | ok |
| drain/reference.md:940 | `git log --grep='^drain: ...'` | read/log-filter | ok |
| drain/reference.md:1228 | prose `grep -q` / `grep -c` guidance | prose | ok |
| gate/reference.md:33 | `grep -qE '^(DEFERRED\|BLOCKED\|INCOMPLETE)'` | read/anchored | ok |
| gate/reference.md:43 | `INPUT=$(cat)` | stdin capture | ok |
| gate/reference.md:53 | `head -1 \| grep -qE '^(...)'` | read/bounded | ok |
| gate/reference.md:172 | prose `sed -i` / `cat >` deny-advice | prose/exception | ok |
| gate/reference.md:173 | prose `Bash(sed -i *)` deny-advice | prose/exception | ok |
| workflow-author/reference.md:167 | comment "grep-like scouting" | prose | ok |
| workflow-author/SKILL.md:48 | "grep-like scouting" | prose | ok |
| ctx/SKILL.md:3 | description "grep/Grep fits that" | prose | ok |
| build/SKILL.md:286 | `grep -l '^Status: pending'` | read/bounded | ok |
| build/SKILL.md:292 | `grep -l` prose reference | prose | ok |

## Mirror note

No edits were made to any skill file, so no `antigravity/` skill mirror is
affected (per the task's step 3: mirrors ride the same-commit rule only when
a SKILL.md's embedded command changes). None of the 11 swept files are rule
mirrors. This evidence file is the only artifact produced.
