# Evidence: workflow-author task 01

## R6 premise re-verification (2026-07-03)

Premise under test: plugins cannot ship workflows — `workflows/*.js`
resolve from project and global scope only, never from a plugin manifest.

Checks performed at implementation time:

1. `code.claude.com/docs/en/plugins-reference` (fetched 2026-07-03): the
   plugin component types are skills, agents, hooks, MCP servers, LSP
   servers, monitors, and themes. A grep of the fetched page for
   "workflow" returned zero matches — workflows remain absent from the
   plugin component paths and the manifest's component path fields.
2. `code.claude.com/docs/en/workflows.md` (fetched 2026-07-03): saved
   workflows load from `.claude/workflows/` in the project (shared via the
   repo; in monorepos every `.claude/workflows/` between cwd and the repo
   root loads, closest name wins) and from `~/.claude/workflows/` in the
   home directory, and run as `/<name>`. Project and user scope only; no
   plugin location is listed.

Conclusion: the premise HOLDS as of 2026-07-03. Plugins still cannot ship
workflows, so a skill that writes the script into the consuming repo's
`.claude/workflows/` remains the distribution path. R6's correction branch
(plugins CAN ship workflows → note both paths, fix the spec's Problem
sentence and the R5 docs entry) is NOT taken; no wording changes needed.

Resolution-by-name is confirmed from the workflows doc above (saved
scripts run as `/<name>` from either scope). The live end-to-end — a
plugin-installed skill writing a workflow into a scratch repo in a fresh
session — is the spec-level manual acceptance criterion ("manual dry-read
until the eval harness covers it") and is not re-run per task.

## Verifier report

Verified 2026-07-03 on branch `task/01-skill-and-templates` (worktree
`/Users/sjaconette/claude/.claude/worktrees/agent-a736da077bf5715ae`),
independent of the implementer. Scope: R1, R2, R3, R4, R6 of
`specs/workflow-author/SPEC.md` per the task file. R5, R7, R8, and the
plugin.json bump are explicitly out of scope for this task.

Verdict: PASS

### Acceptance commands (run exactly as written, from repo root)

1. R1 — `test -f .claude/skills/workflow-author/SKILL.md && [ "$(wc -l < .claude/skills/workflow-author/SKILL.md)" -le 100 ] && ! grep -q "^disable-model-invocation" .claude/skills/workflow-author/SKILL.md && grep -q "human-gates" .claude/skills/workflow-author/SKILL.md`
   -> exit 0. SKILL.md is 66 lines; no line-anchored
   `disable-model-invocation`; "human-gates" present. PASS
2. R3 — `grep -q "sole writer" ... && grep -q "BLOCKED" ... && grep -q "budget.remaining()" .claude/skills/workflow-author/SKILL.md`
   -> exit 0. All three marker phrases present. PASS
3. R4 — `test -f .claude/skills/workflow-author/reference.md && grep -q "tournament.js" ... && grep -q "queue-wave.js" ... && grep -q "tournament-votes" ... && grep -q "export const meta" .claude/skills/workflow-author/reference.md`
   -> exit 0. PASS
4. R6 — `test -f specs/workflow-author/evidence/01.md` -> exit 0, and the
   file records a substantive premise re-verification (see section above:
   both docs checked 2026-07-03, premise HOLDS, correction branch not
   taken). PASS

### Spec spot-checks (beyond the greps)

- R1: description is third person ("Turns a repeated multi-agent
  orchestration into...") and contains all four trigger phrases verbatim
  ("save this as a workflow", "make this orchestration repeatable",
  "write an ultracode workflow", "turn this into a workflow script").
  Body sentence at SKILL.md lines 10-13 cites docs/human-gates.md reason 5
  and states the double gate (ultracode opt-in + invocation by name). PASS
- R2: exactly five numbered steps (Qualify / Write the script / Apply the
  doctrine guards / Validate / Hand off) matching the spec's enumeration,
  including decline-with-explanation, pure-literal meta, pipeline() by
  default, parallel() barrier-justification comments, the
  Date.now()/Math.random()/argless-new-Date() validation, schema over
  prose-parsing, and the hand-off wording. PASS
- R3: all four guards (single writer, BLOCKED routing, budget, untrusted
  returns) are stated in SKILL.md's "Doctrine guards" section AND
  demonstrated in BOTH templates: each template opens with a SOLE WRITER
  header comment warning against a concurrent attended /drain and stating
  disk-resumability; both route BLOCKED (skip remaining stages, quote
  blocked content verbatim in the final return); both guard fan-out on
  `budget.remaining()` with the budget noted as human-set; both carry the
  untrusted-returns statement. PASS
- R4: API summary spans reference.md lines 8-32 = 25 lines (<=25) and
  contains every enumerated item: plain JS + pure-literal meta;
  agent(prompt, opts) -> Promise of final text or schema-validated object
  with the full opts list (label, phase, schema, model, effort,
  isolation: 'worktree', agentType); parallel() is a barrier /
  pipeline() is not; phase()/log(); args verbatim; budget.total/spent()/
  remaining(); the three throwing nondeterminism sources; and the
  load-bearing SCRIPTS HAVE NO FILESYSTEM ACCESS statement. Both
  templates are fenced illustrative code. tournament.js's sole
  parallel() call carries a justification comment naming the cross-item
  dependency (ranking compares candidates); queue-wave.js uses pipeline()
  only, so no barrier comment is required. tournament.js cites
  specs/tournament-votes/SPEC.md as the vote-design owner ("that spec owns
  the vote design — cite it, never redefine it"); its inline summary
  (majority PASS 2-of-3, FAIL/INCOMPLETE non-PASS, verifier-BLOCKED
  disqualifies with quote recorded) matches the owning spec, checked
  against specs/tournament-votes/SPEC.md lines 28-31/48-53. PASS
- R6: recorded above; substantive (specific component lists and
  resolution scopes, dated). Caveat: the doc fetches are the
  implementer's recorded observations and are not independently
  reproducible from the repo (this verifier has no web access); the
  criterion requires the re-verification be recorded, which it is.

### Standard gates

- `for t in tests/test_*.sh; do bash "$t"; done` — 3/3 PASS
  (test_hook_templates, test_install_gates, test_sync_skills).
- `./evals/runner-selftest.sh` — exit 0, "runner selftest: OK".

### Scope / overfitting findings

- Diff vs HEAD: only the three Touch-list files (all new) plus a
  16-line HTML plan comment in the task file itself (plan recording is
  required by the repo workflow; Status: in-progress was committed
  earlier at 6ed742e). No other files touched. No scope creep.
- plugin.json intentionally NOT bumped — CLAUDE.md's convention says to
  bump on skill changes, but the task file assigns the bump to rf-99 and
  the caller marks R8 out of scope; noted, not a failure.
- No overfitting: the acceptance greps are marker-phrase checks, and the
  files carry full genuine content behind each marker (the spec notes
  the markers did not previously exist in the repo, confirmed — they
  appear only in this spec set and the new files). No test files were
  modified.
