# Verification report: Task 02 — refresh script and launchd assets

Verdict: PASS

Base commit: bae759e53c899595e7240c3535fbe3f508d2dd01
Repo: /Users/sjaconette/claude/.claude/worktrees/agent-a21eb63534cdff2ca (agentprof/)

## Touch constraint check

`git diff bae759e53c899595e7240c3535fbe3f508d2dd01 --name-only`:

```
agentprof/launchd/com.sjaconette.agentprof-pprof.plist.tmpl
agentprof/launchd/com.sjaconette.agentprof-refresh.plist.tmpl
agentprof/scripts/refresh-profile.sh
agentprof/scripts/serve-pprof.sh
```

All four files fall under `agentprof/scripts/` or `agentprof/launchd/` as required by the
task's Touch line. No other paths touched (no `agentprof/internal/`, no `agent-console/`,
no `.claude/`). PASS.

## Personal-data / hardcoded-username sweep

Command: `grep -rn "sjaconette\|Jaconette" agentprof/scripts/ agentprof/launchd/`

```
agentprof/launchd/com.sjaconette.agentprof-pprof.plist.tmpl:9:    <string>com.sjaconette.agentprof-pprof</string>
agentprof/launchd/com.sjaconette.agentprof-refresh.plist.tmpl:9:    <string>com.sjaconette.agentprof-refresh</string>
```

Only the two documented `com.sjaconette.*` launchd `Label` strings appear. No hits in
`agentprof/scripts/` at all — the scripts derive HOME at runtime (`$HOME`) rather than
hardcoding it. PASS.

## Acceptance criteria

### 1. Refresh script produces a nonempty profile

Command:
```
bash agentprof/scripts/refresh-profile.sh && test -s ~/.local/state/agentprof/claude-30d.pb.gz
```
Output: exit 0. `ls -la ~/.local/state/agentprof/` showed a 596936-byte
`claude-30d.pb.gz` (mode 0600) after the run. Note: `~/.local/state/agentprof` did NOT
exist before this run — the script created it via `mkdir -p`, confirming the
state-dir-creation behavior described in the Goal.

Verdict: PASS

### 2. serve-pprof responds on 8901 with real pprof UI HTML

Command:
```
bash agentprof/scripts/serve-pprof.sh &
sleep 2
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8901/   # -> 307
curl -sL http://127.0.0.1:8901/ | head -c 300                      # follow redirect
kill %1
```
Output:
- Plain `curl -s .../` yields HTTP 307 (redirect to `/ui`) — this is pprof's own server
  behavior, not a bug.
- `curl -sL` (follow redirects) returns a real HTML fragment:
  `<!DOCTYPE html><html><head><meta charset="utf-8"><title>unknown cost_microusd</title>...`
  i.e. the actual pprof web UI page.
- Process was killed after the check; `go tool pprof: signal: terminated` confirms clean
  termination. `lsof -i :8901` afterward showed no listener.

Per the task-runner's guidance, the 307-then-real-HTML-via--L result is treated as a PASS
for this criterion (documented pprof behavior, not a script defect).

Verdict: PASS

### 3. Both launchd plist templates exist and lint clean after substitution

Command: `ls agentprof/launchd/com.sjaconette.agentprof-refresh.plist.tmpl agentprof/launchd/com.sjaconette.agentprof-pprof.plist.tmpl`
Output: both files listed (exist).

Substitution used (sed, placeholders replaced with representative values):
```
sed -e "s|__HOME__|$HOME|g" -e "s|__PATH__|/usr/bin:/bin|g" \
    -e "s|__SCRIPT__|$PWD/agentprof/scripts/refresh-profile.sh|g" \
    -e "s|__LOG_DIR__|$HOME/Library/Logs/agentprof-refresh|g" \
    agentprof/launchd/com.sjaconette.agentprof-refresh.plist.tmpl > refresh.plist

sed -e "s|__HOME__|$HOME|g" -e "s|__PATH__|/usr/bin:/bin|g" \
    -e "s|__SCRIPT__|$PWD/agentprof/scripts/serve-pprof.sh|g" \
    -e "s|__LOG_DIR__|$HOME/Library/Logs/agentprof-pprof|g" \
    agentprof/launchd/com.sjaconette.agentprof-pprof.plist.tmpl > pprof.plist

plutil -lint refresh.plist   # -> OK
plutil -lint pprof.plist     # -> OK
```
Output:
```
/var/folders/.../refresh.plist: OK
/var/folders/.../pprof.plist: OK
```

Verdict: PASS

### 4. No committed (non-template) plist files

Command: `find agentprof -name "*.plist" ! -name "*.tmpl" | wc -l`
Output: `0`

Verdict: PASS

### 5. `agentprof/scripts/check.sh` green

Command: `cd agentprof && bash scripts/check.sh`
Output:
```
check: format-check ok
check: lint ok
check: tests ok
```
Exit code: 0

Verdict: PASS

## Cleanup performed

- Removed `~/.local/state/agentprof/claude-30d.pb.gz` (generated by criterion-1 run).
- Removed the now-empty `~/.local/state/agentprof` directory (confirmed gone via `ls`
  error "No such file or directory").
- Confirmed no process listening on port 8901 after killing the criterion-2 server
  (`lsof -i :8901` returned nothing).

## Scope-creep check

`git diff bae759e53c899595e7240c3535fbe3f508d2dd01 --stat` shows only the 4 new files
under the allowed Touch paths (128 insertions, 0 deletions, no modifications to existing
files). No scope creep found.

## Summary

All 5 acceptance criteria PASS. Touch constraint satisfied. No hardcoded username outside
the documented `com.sjaconette.*` launchd Label strings. `check.sh` green. No scope creep
detected in the diff.
