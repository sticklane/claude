# Task 06: complete the antigravity/AGENTS.md token-discipline mirror

<!-- Machine-read fields (Status, Depends on, Priority, Budget, Touch) are single-line `Key: value` headers above the first ## heading; body sections are never parsed by orchestrators. -->
Status: done
Depends on: none
Spec: ../SPEC.md
Discovered-by: 01-dispatch-authoring-rule.md

## Resolution — closed as out-of-scope (2026-07-03)

No work performed; this discovered stub was closed after review. The
premise (a mirror-drift gap) does not hold: `antigravity/` mirrors
**skills, agents, and hooks** only — per the CLAUDE.md invariant
("skills near-identical, agents→skills, human-only skills→workflows,
hooks in Antigravity's JSON shape"). `.claude/rules/` is **not** a
mirrored category; antigravity has no `rules/` dir and no
`token-discipline.md`. The two sections the worker flagged ("Cheap
before expensive", "Match the research tool to the question") therefore
have no expected antigravity counterpart, so there is nothing to
reconcile. The worker itself flagged this as "outside this task's
scope."

## Original goal (superseded)

verbatim worker report — vet/rewrite before promoting:

> The antigravity/AGENTS.md token-discipline mirror is incomplete — the
> rule's "Cheap before expensive" and "Match the research tool to the
> question" sections have no antigravity counterpart; a pre-existing
> mirror-drift gap outside this task's scope.
