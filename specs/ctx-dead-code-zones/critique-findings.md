# Critique findings — READY WITH NITS (2026-07-21, round 1; nits applied same day, NOT re-verified)

No content hash ON PURPOSE — next /critique must re-run.

Round-1 nits (all applied): (1) no-match vs filtered-empty seam with
ctx-absence-check — boundary sentence added to both specs (no-match =
resolution failure only; filtered-empty exits 0 with tail); (2)
.ctxzones line-grammar edges pinned (label charset, multi-line union,
first-match-wins, unknown --zone errors exit 2); (3) Landing-order
slot 5 + token-doctrine R7 hard dependency declared; (4) R5 now cites
fooszone repo-orientation-hygiene (REVISED 2026-07-21) and is a no-op
pointer; (5) exit codes + golden command list pinned in R3.
