# Task 01: dangling-dep fixture

Status: pending
Depends on: 99
Priority: P2
Touch: fixture/only

## Goal

Fixture task whose `Depends on: 99` parses as a `number`-kind dependency but
resolves to no task numbered 99 in this fixture spec — exercising
`compute_frontier`'s `unresolved-external-dep` diagnostic branch (not the
`FrontierError` path a bare word-token would take). Used by
`specs/cheap-task-status-checks` task 01 to prove `drain_frontier.py
--strict` fails the run on a dangling reference.
