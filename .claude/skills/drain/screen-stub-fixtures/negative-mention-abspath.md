# Task 08: RULE-ISOLATED negative — descriptive path mention

Status: draft

## Original report

> claude.go:673 result-matching skips tool_result blocks on
> IsMeta/IsSidechain user lines before populating toolResultIDs —
> suspected mechanical source of the pending-sample volume. Confirm
> against real ~/.claude transcripts (needs $HOME data, outside an
> isolated worktree) and fix the matching if confirmed; record
> before/after counts in evidence/.
