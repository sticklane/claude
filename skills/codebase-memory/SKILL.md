---
name: codebase-memory
description: Answers code-structure questions with the repository's Codebase-Memory graph before bounded source fallback—architecture, qualified symbols, callers, dependencies, impact, snippets, and coverage-aware negative claims.
---

Read and follow [the canonical workflow](../../.claude/skills/codebase-memory/SKILL.md) completely before acting. Resolve every relative link in that file against its own directory. This packaging entrypoint contains no workflow procedure; the linked file remains the single source of truth.
