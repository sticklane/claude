This static-site generator has date-formatting logic duplicated across several modules, and the copies have drifted — at least one renders certain dates wrong. Unify the duplicates into a single shared module and migrate every call site to it. Rendered output for the bundled sample site must stay identical, except where the buggy copy produced wrong dates — those must now be correct. All tests must stay green; add coverage pinning the corrected behavior.
