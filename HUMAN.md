# Human-actionable items

## Agent-filed blockers

- [ ] 2026-07-21 · specs/agentprof-skill-audit/tasks/04-cli-wiring-and-report.md · run — run `agentprof skillcheck` against your real `~/.claude/projects/-Users-sjaconette-claude/*.jsonl` history (the agentprof CLI's new subcommand that scores whether skills were triggered correctly and whether they succeeded) and manually read at least 5 of its verdicts to confirm they look plausible; then run it again against a `SKILL.md` that has a custom `outcome-rubric:` field and confirm that skill's findings reflect the custom rubric having been used rather than the generic one. Both need a live judge subprocess call against real history, which an unattended worker can't do. — Blocks: no other pending task (the two automated-proof acceptance criteria this pairs with are already done; this is the real-world sanity check for both) — Still-blocked: none — the check needs a live judge subprocess over real transcript history plus a human reading five verdicts for plausibility, and nothing on disk records that a human read them
- [ ] 2026-07-21 · specs/agentprof-otel-ingestion/SPEC.md · run — capture a live Claude Code OTel session (CLI ≥ v2.1.212 with `CLAUDE_CODE_ENABLE_TELEMETRY=1` and `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1` pointed at a local OTLP receiver, e.g. `agentprof otel serve`) and check which span the `claude_code.api_request` log event's `span_id` actually targets — the `llm_request` span that carries the tokens, or the enclosing `interaction` span. The spec's cost-attach rule was designed from docs and fixtures; only a real capture confirms it matches what the CLI emits. — Blocks: Phase-2 hardening of the OTel cost join (Phase 1 proceeds on golden fixtures) — Still-blocked: none — the answer only exists in a live capture against a running receiver, and a capture nobody took leaves no trace a probe could read
- [ ] 2026-07-22 · specs/agentprof-skill-audit/tasks/07-skill-invocation-source-line.md · decide — agentprof's skill-invocation report currently cites findings by file only (e.g. `some/file.py`), not by file-and-line, because the underlying data model doesn't capture where in the transcript a skill was invoked. Decide whether to approve adding that (source line + the exact user message that triggered it) so findings can cite `file:line` precisely, and if so what the acceptance test should check — the task file has a full technical description but no test criteria yet. — Blocks: no other pending task — Still-blocked: none — an unmade product decision leaves no observable state; the signal is the task file gaining acceptance criteria, which only the decision produces
- [ ] 2026-07-22 · specs/agentprof-skill-audit/tasks/08-possible-miss-false-positive-check.md · decide — agentprof's "possible miss" detector (flags a conversation turn as maybe having needed a skill that didn't fire) can currently flag a turn even when the matching skill *was* used later in the same conversation — a real false-positive bug, confirmed. Fixing it directly conflicts with this spec's own written scope ("no fix to the possible-miss detector's precision" — a deliberate earlier decision to skip this class of work), so it needs a human call on whether to reopen that scope, and if so, exactly when a turn should NOT count as a miss: skill used anywhere in the conversation, that specific turn triggered something, or only if the skill ran afterward. The task file has the full analysis. — Blocks: no other pending task — Still-blocked: none — reopening a written scope is a human judgement with no precondition to measure; the signal is the spec's own scope line changing
- [ ] 2026-07-25 · bd issue agentic-hty (see also agentic-kts) · decide — decide how to repair the beads queue in ~/ynab-mcp-new, whose `bd export` writes an EMPTY file while the database holds 13 real issues. The export is the committed, readable copy of that repo's task queue, so right now nothing outside bd can see its work. bd's own two suggested remedies (`bd init --force`, `bd init --server`) both re-initialize a live 13-issue queue, which risks losing it — so this needs your call rather than an agent's: migrate that repo to bd server mode, or leave it embedded and fix/report the export bug upstream first. Do not let an agent re-init it unasked. — Blocks: finishing the beads install in ~/ynab-mcp-new (issue agentic-hty, whose other half is already done and committed); also leaves the silent-empty-export bug agentic-kts unconfirmed on other embedded-mode repos — Still-blocked: ynab-mcp-new-bd-export
- [ ] 2026-07-26 · specs/mardi-gras-agentic-integration/tasks/07-prove-installed-native-facades.md and specs/mardi-gras-agentic-integration/tasks/26-prove-final-installed-native-facades.md · run — sign into real Claude Code, Codex, and Antigravity sessions and drive the native façade canary once per runtime against the installed immutable package, recording claim-through-cleanup evidence for each (Claude Code and Codex cover work/build/drain; Antigravity covers work/build and must record drain and resume as rejected before spawn), then run the same matrix again against the FINAL immutable package so the certification evidence is authenticated against the shipped bytes. One sitting can satisfy both task files: task 07 is the pass against the currently installed package, task 26 the confirming pass against the final one. Both mark themselves MANUAL-PENDING because an agent cannot authenticate three native runtime managers; the exact commands are in each task file's Acceptance list. — Blocks: certification of the production cutover (tasks 12 and 27), which reads the reports these produce — Still-blocked: none — no probe can attest that three authenticated runtime sessions were driven; the report files' own existence and `complete: true` flags are the only signal, and re-running the canary is the only way to produce them
- [ ] 2026-07-26 · specs/mardi-gras-agentic-integration/tasks/12-certify-production-cutover.md and specs/mardi-gras-agentic-integration/tasks/27-certify-production-cutover-repair.md · run — certify the production cutover end to end once trusted upstream releases exist to install: reinstall the current package from the released Beads and Mardi Gras artifacts, run the read-only live check, drive the façade matrix and the full terminal journey against a released binary, then call `agentic live --record-cutover`, confirm `--cutover-status` reports ready, and keep the cutover receipt. Both task files ask for this same certification (task 27 is the repair-wave re-run of task 12) and both are MANUAL-PENDING on the same external dependency: nothing in this repo can cause an upstream Mardi Gras release advertising the exact R14 handshake. — Blocks: nothing else pending in this spec; it is the terminal certification gate for the production control cutover — Still-blocked: none — gated on a third-party release, so the check is whether upstream has shipped a build advertising the R14 handshake
