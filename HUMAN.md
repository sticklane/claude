# Human-actionable items

## Agent-filed blockers

- [ ] 2026-07-12 · specs/agentprof-scrub-hex-tokens/SPEC.md · run — rotate the Todoist API token pasted into a 2026-07-11 automation-session prompt (it sits verbatim in that transcript and in generated agentprof profiles; after rotating, re-run the agentprof-refresh job so `~/.local/state/agentprof/*` regenerates without it)
