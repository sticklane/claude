# Human-actionable items

## Agent-filed blockers

- [ ] 2026-07-13 · specs/trajectory-evals/critique-findings.md · decide — NOT READY (2nd round, approved edits applied); new: R4 and R5 now directly contradict each other — R5 (as approved) says the codex mirror's "never a transcript" line must be updated as contradicting v2, but that line is actually about an unrelated assert.sh budget, and R4 explicitly requires that same line be kept (conf 85, the approved edit list carried forward a misread from the original critique). Note 2026-07-14: another live session appears to be actively fixing this one already (uncommitted edits observed in the shared checkout) — check its state before starting fresh work here. — Blocks: breakdown of this spec into dispatchable tasks
- [ ] 2026-07-21 · specs/agentprof-skill-audit/tasks/04-cli-wiring-and-report.md · run — run `agentprof skillcheck` against your real `~/.claude/projects/-Users-sjaconette-claude/*.jsonl` history (the agentprof CLI's new subcommand that scores whether skills were triggered correctly and whether they succeeded) and manually read at least 5 of its verdicts to confirm they look plausible; then run it again against a `SKILL.md` that has a custom `outcome-rubric:` field and confirm that skill's findings reflect the custom rubric having been used rather than the generic one. Both need a live judge subprocess call against real history, which an unattended worker can't do. — Blocks: no other pending task (the two automated-proof acceptance criteria this pairs with are already done; this is the real-world sanity check for both)
- [ ] 2026-07-21 · specs/agentprof-otel-ingestion/SPEC.md · run — capture a live Claude Code OTel session (CLI ≥ v2.1.212 with `CLAUDE_CODE_ENABLE_TELEMETRY=1` and `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1` pointed at a local OTLP receiver, e.g. `agentprof otel serve`) and check which span the `claude_code.api_request` log event's `span_id` actually targets — the `llm_request` span that carries the tokens, or the enclosing `interaction` span. The spec's cost-attach rule was designed from docs and fixtures; only a real capture confirms it matches what the CLI emits. — Blocks: Phase-2 hardening of the OTel cost join (Phase 1 proceeds on golden fixtures)
- [ ] 2026-07-22 · specs/agentprof-skill-audit/tasks/07-skill-invocation-source-line.md · decide — agentprof's skill-invocation report currently cites findings by file only (e.g. `some/file.py`), not by file-and-line, because the underlying data model doesn't capture where in the transcript a skill was invoked. Decide whether to approve adding that (source line + the exact user message that triggered it) so findings can cite `file:line` precisely, and if so what the acceptance test should check — the task file has a full technical description but no test criteria yet. — Blocks: no other pending task
- [ ] 2026-07-22 · specs/agentprof-skill-audit/tasks/08-possible-miss-false-positive-check.md · decide — agentprof's "possible miss" detector (flags a conversation turn as maybe having needed a skill that didn't fire) can currently flag a turn even when the matching skill *was* used later in the same conversation — a real false-positive bug, confirmed. Fixing it directly conflicts with this spec's own written scope ("no fix to the possible-miss detector's precision" — a deliberate earlier decision to skip this class of work), so it needs a human call on whether to reopen that scope, and if so, exactly when a turn should NOT count as a miss: skill used anywhere in the conversation, that specific turn triggered something, or only if the skill ran afterward. The task file has the full analysis. — Blocks: no other pending task
- [ ] 2026-07-25 · bd issue agentic-hty (see also agentic-kts) · decide — decide how to repair the beads queue in ~/ynab-mcp-new, whose `bd export` writes an EMPTY file while the database holds 13 real issues. The export is the committed, readable copy of that repo's task queue, so right now nothing outside bd can see its work. bd's own two suggested remedies (`bd init --force`, `bd init --server`) both re-initialize a live 13-issue queue, which risks losing it — so this needs your call rather than an agent's: migrate that repo to bd server mode, or leave it embedded and fix/report the export bug upstream first. Do not let an agent re-init it unasked. — Blocks: finishing the beads install in ~/ynab-mcp-new (issue agentic-hty, whose other half is already done and committed); also leaves the silent-empty-export bug agentic-kts unconfirmed on other embedded-mode repos
