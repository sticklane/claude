# Human-actionable items

## Agent-filed blockers

- [ ] 2026-07-13 · specs/trajectory-evals/critique-findings.md · decide — NOT READY (2nd round, approved edits applied); new: R4 and R5 now directly contradict each other — R5 (as approved) says the codex mirror's "never a transcript" line must be updated as contradicting v2, but that line is actually about an unrelated assert.sh budget, and R4 explicitly requires that same line be kept (conf 85, the approved edit list carried forward a misread from the original critique). Note 2026-07-14: another live session appears to be actively fixing this one already (uncommitted edits observed in the shared checkout) — check its state before starting fresh work here. — Blocks: breakdown of this spec into dispatchable tasks
- [ ] 2026-07-19 · specs (5 dirs, 9 task files) · decide — legalize `Status: obsolete` in the task-status vocabulary or map those 9 tasks to `skipped`: mirror-procedure-discipline (16, 20, 21), orchestrator-share-audit (03), shared-viz-renderer (07–09), skill-doc-size-guards (06), spec-completion-review (04) all use it for "closed without implementation", the scanner flags them as unrecognized, and two carry Intake-refused notes disputing the label.
- [ ] 2026-07-21 · specs/agentprof-skill-audit/tasks/04-cli-wiring-and-report.md · run — run `agentprof skillcheck` against your real `~/.claude/projects/-Users-sjaconette-claude/*.jsonl` history (the agentprof CLI's new subcommand that scores whether skills were triggered correctly and whether they succeeded) and manually read at least 5 of its verdicts to confirm they look plausible; then run it again against a `SKILL.md` that has a custom `outcome-rubric:` field and confirm that skill's findings reflect the custom rubric having been used rather than the generic one. Both need a live judge subprocess call against real history, which an unattended worker can't do. — Blocks: no other pending task (the two automated-proof acceptance criteria this pairs with are already done; this is the real-world sanity check for both)
- [ ] 2026-07-21 · specs/agentprof-otel-ingestion/SPEC.md · run — capture a live Claude Code OTel session (CLI ≥ v2.1.212 with `CLAUDE_CODE_ENABLE_TELEMETRY=1` and `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1` pointed at a local OTLP receiver, e.g. `agentprof otel serve`) and check which span the `claude_code.api_request` log event's `span_id` actually targets — the `llm_request` span that carries the tokens, or the enclosing `interaction` span. The spec's cost-attach rule was designed from docs and fixtures; only a real capture confirms it matches what the CLI emits. — Blocks: Phase-2 hardening of the OTel cost join (Phase 1 proceeds on golden fixtures)
- [ ] 2026-07-22 · specs/agentprof-skill-audit/tasks/07-skill-invocation-source-line.md · decide — agentprof's skill-invocation report currently cites findings by file only (e.g. `some/file.py`), not by file-and-line, because the underlying data model doesn't capture where in the transcript a skill was invoked. Decide whether to approve adding that (source line + the exact user message that triggered it) so findings can cite `file:line` precisely, and if so what the acceptance test should check — the task file has a full technical description but no test criteria yet. — Blocks: no other pending task
- [ ] 2026-07-22 · specs/agentprof-skill-audit/tasks/08-possible-miss-false-positive-check.md · decide — agentprof's "possible miss" detector (flags a conversation turn as maybe having needed a skill that didn't fire) can currently flag a turn even when the matching skill *was* used later in the same conversation — a real false-positive bug, confirmed. Fixing it directly conflicts with this spec's own written scope ("no fix to the possible-miss detector's precision" — a deliberate earlier decision to skip this class of work), so it needs a human call on whether to reopen that scope, and if so, exactly when a turn should NOT count as a miss: skill used anywhere in the conversation, that specific turn triggered something, or only if the skill ran afterward. The task file has the full analysis. — Blocks: no other pending task
- [ ] 2026-07-22 · docs/cross-repo-rollout-2026-07-22.md · decide — decide whether ~/automation should adopt the beads (bd) issue tracker or keep tracking work in docs/TASKS.md. The 2026-07-03 "full exit" moved every repo to markdown docs/TASKS.md; the 2026-07-22 pivot reversed that for the ~/claude toolkit repo only, so automation needs its own call. If adopting bd, follow specs/beads-daily-skill/SPEC.md "Installation in other repos" (bd init curated, /gate with the bd-compliance check, Bash(bd *) allowlist, seed the queue) and update CLAUDE.md line 14, which currently reads "Task tracking: docs/TASKS.md". — Blocks: automation settling on a deliberate, non-defaulted work-tracking choice after the pivot
- [ ] 2026-07-22 · docs/cross-repo-rollout-2026-07-22.md · decide — reconcile ~/fooszone's contradictory task-tracking docs. CLAUDE.md says "tracker decommissioned 2026-07" and to use docs/TASKS.md, while AGENTS.md still mandates "Use bd for ALL task tracking—do NOT use ... markdown TODO lists". Decide which tracker fooszone uses (bd or markdown), then edit both files so they agree; if choosing bd, follow specs/beads-daily-skill/SPEC.md "Installation in other repos". — Blocks: fooszone agents getting one consistent tracking instruction instead of two that contradict each other
- [ ] 2026-07-22 · docs/cross-repo-rollout-2026-07-22.md · decide — decide whether ~/interview-prep should re-adopt beads (bd) or keep docs/TASKS.md. Its AGENTS.md line 41 still records "Issue tracker (beads)—decommissioned 2026-07" from the 2026-07-03 full exit, which the 2026-07-22 pivot reversed for the toolkit repo only. If adopting bd, follow specs/beads-daily-skill/SPEC.md "Installation in other repos" and update that decommissioned-tracker note. — Blocks: interview-prep settling on a deliberate work-tracking choice after the pivot
- [ ] 2026-07-22 · docs/cross-repo-rollout-2026-07-22.md · decide — decide whether ~/portfolio-tracker should re-adopt beads (bd) or keep docs/TASKS.md. Its CLAUDE.md line 172 records "tracker decommissioned 2026-07" from the 2026-07-03 full exit, which the 2026-07-22 pivot reversed for the toolkit repo only. If adopting bd, follow specs/beads-daily-skill/SPEC.md "Installation in other repos" and update that decommissioned-tracker note. — Blocks: portfolio-tracker settling on a deliberate work-tracking choice after the pivot
- [ ] 2026-07-25 · bd issue agentic-hty (see also agentic-kts) · decide — decide how to repair the beads queue in ~/ynab-mcp-new, whose `bd export` writes an EMPTY file while the database holds 13 real issues. The export is the committed, readable copy of that repo's task queue, so right now nothing outside bd can see its work. bd's own two suggested remedies (`bd init --force`, `bd init --server`) both re-initialize a live 13-issue queue, which risks losing it — so this needs your call rather than an agent's: migrate that repo to bd server mode, or leave it embedded and fix/report the export bug upstream first. Do not let an agent re-init it unasked. — Blocks: finishing the beads install in ~/ynab-mcp-new (issue agentic-hty, whose other half is already done and committed); also leaves the silent-empty-export bug agentic-kts unconfirmed on other embedded-mode repos
- [ ] 2026-07-26 · tests/test_bd_compliance_hook.sh · decide — choose whether the bd-compliance root test may move from the immutable `retain` inventory class to `repair`, allowing its parity and distributable-suite coverage to evolve, or require issue agentic-00w to add a new inventoried root entrypoint instead. The implemented branch passes both hook suites but fails the frozen-content inventory check. — Blocks: agentic-00w (keeping the installed and distributable bd-compliance hooks behaviorally tested and byte-identical)
- [ ] 2026-07-26 · tests/test_check_sh_runner.sh · decide — choose whether the check-runner root test may move from the immutable `retain` inventory class to `repair`, allowing the task-mandated within-process `SERIAL` regression, or change issue agentic-5dw’s acceptance design so that retained file stays byte-identical. The implementation branch passes the runner and load-invariant latency tests but fails the frozen-content inventory check. — Blocks: agentic-5dw (documenting and enforcing the check runner’s within-process-only serialization contract)
- [ ] 2026-07-26 · tests/test_token_discipline_structure.sh · decide — choose whether issue agentic-cz8 should move its new audit-deliverable regression into a separate additively inventoried test, preserving this immutable `retain` file byte-for-byte, or reclassify the existing structural test to `repair`. Its doctrine and authoring/readiness changes pass focused checks, but the canonical gate rejects the retained-file edit. — Blocks: agentic-cz8 (requiring verifier-readable, scoped on-disk outputs for every audit or finding task)
- [ ] 2026-07-26 · tests/test_check_sh_runner.sh · decide — choose whether issue agentic-7c7 may revise its acceptance criterion to put the new four-test parallelism regression in a separate additively inventoried test, or reclassify the existing immutable `retain` runner test to `repair`. The required fixture is absent on the clean base, so the current criterion cannot pass without violating the frozen-content inventory. — Blocks: agentic-7c7 (parallelizing the repository check runner while preserving serial-test ordering)
- [ ] 2026-07-26 · tests/test_session_refresh_hook.sh · decide — choose whether the passing session-refresh root wrapper should remain an immutable `retain` audit record, move to `repair`, or be replaced by a new additively inventoried wrapper when future delegation behavior changes. — Blocks: agentic-pvl (future maintenance of the session-refresh root-to-hook-local test bridge); no currently running implementation
- [ ] 2026-07-26 · tests/test_session_refresh_hook.sh · decide — choose whether agentic-3pj may retire or reclassify this immutable `retain` wrapper so it can become pure delegation to the authoritative hook-local suite, or revise acceptance criterion 2 to preserve the existing duplicated sidechain checks. The branch’s installed-hook, inventory, installer, template, and parity tests pass; only the frozen wrapper contract blocks completion. — Blocks: agentic-3pj (direct behavior/parity coverage for every installed hook and one canonical hook source)
